# Railway uses Nixpacks to build Node projects. Nothing to configure
# at the build step — `npm install` pulls `serve`, and `npm start` runs
# it against ui_kits/portfolio_site/ on the port Railway provides.
NODE_ENV=production
